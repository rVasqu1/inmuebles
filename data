// 🔹 Espera a que se actualicen los datos filtrados
    await new Promise(resolve => oTable.attachEventOnce("updateFinished", resolve));

    // 🔹 Obtén los datos visibles en la tabla
    const aItems = oBinding.oList || oBinding.aIndices.map(i => oBinding.oList[i]);
    const aData = aItems.map(oContext => oContext.getObject());
