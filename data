Método ABAP que procesa la tabla interna MT_REPARTOS para actualizar porcentajes de refacturación.

Filtra registros de sociedad 0004 con expenses_refact > 0.

Valida la existencia de refact_society en T001 mediante FI_COMPANY_CODE_CHECK.

Genera nuevos registros en LT_RSNAMSА con UUID y metadatos (usuario, fecha, hora).

Ajusta porcentajes si la sociedad es REGE consultando ZFI50.

Recalcula expenses_refact usando fórmula: exp = exp / (1 - (temp_exp / 100)).

Valida que los totales por cost_center sean exactamente 100%.

Genera registros complementarios con refact_society = gc_refact_society_no_rege.

Registra errores en MT_ERRORS usando la clase ZCX_RE_UPDATE_REPARTOS.

Sustituye MT_REPARTOS por los valores procesados de LT_RSNAMSА.
