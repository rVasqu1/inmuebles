oFileUpload: function (oEvent) {

    var oFile = oEvent.getParameter("files")[0];
    if (!oFile) return;

    var reader = new FileReader();

    reader.onload = function (e) {

        var data = new Uint8Array(e.target.result);
        var workbook = XLSX.read(data, { type: "array" });
        var sheet = workbook.Sheets[workbook.SheetNames[0]];

        // leer excel como matriz
        var aExcel = XLSX.utils.sheet_to_json(sheet, { header: 1 });

        if (!aExcel.length) {
            sap.m.MessageToast.show("Excel vacío");
            return;
        }

        var aHeaders = aExcel[0];
        var aRows = aExcel.slice(1);

        var oTable = this.byId("tableNaturaleza");

        // obtener columnas de la tabla
        var aColumns = oTable.getColumns().map(function (oCol) {

            var label = "";
            if (oCol.getLabel()) {
                label = oCol.getLabel().getText();
            }

            var property =
                oCol.getSortProperty() ||
                oCol.getFilterProperty() ||
                "";

            return {
                label: label.trim(),
                property: property
            };

        });

        // crear mapa Excel -> propiedad SAP
        var columnMap = {};

        aHeaders.forEach(function (excelHeader, i) {

            if (!excelHeader) return;

            var header = excelHeader.toString().trim().toLowerCase();

            var match = aColumns.find(function (col) {
                return col.label.toLowerCase() === header;
            });

            if (match) {
                columnMap[i] = match.property;
            }

        });

        var aData = aRows.map(function (row, index) {

            var obj = {
                _numerico: index + 1
            };

            Object.keys(columnMap).forEach(function (excelIndex) {

                var property = columnMap[excelIndex];
                var value = row[excelIndex];

                if (value === undefined) return;

                // FECHA
                if (value instanceof Date) {
                    obj[property] = value;
                }

                // HORA
                else if (typeof value === "string" && value.match(/^\d{2}:\d{2}:\d{2}$/)) {

                    var parts = value.split(":");

                    var ms =
                        (parseInt(parts[0]) * 3600 +
                        parseInt(parts[1]) * 60 +
                        parseInt(parts[2])) * 1000;

                    obj[property] = { ms: ms };

                }

                else {
                    obj[property] = value;
                }

            });

            return obj;

        });

        var oModel = this.getView().getModel("mapfresa");

        // reemplazar datos
        oModel.setProperty("/naturaleza", []);
        oModel.refresh(true);

        oModel.setProperty("/naturaleza", aData);
        oModel.refresh(true);

        sap.m.MessageToast.show("Excel cargado correctamente");

    }.bind(this);

    reader.readAsArrayBuffer(oFile);
}
