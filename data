      // FIN RJVS 09/03/6 Dowload datos excel tableCeco
            dowloadExceltipoCeco: function () {
                var oTable = this.byId("tableTipoCeco");
                // Se obtiene el binding de sus filas
                var oBinding = oTable.getBinding("rows");
                // Se obtiene los contexto de cada fila
                var aContexts = oBinding.getContexts();
                //Se comprueba si tamaño del array de contextos es distinto a 0
                if (!aContexts.length) {
                    sap.m.MessageToast.show("No hay datos para exportar");
                    return;
                }
                //Recorriendo los contextos para obtener los datos
                var aData = aContexts.map(function (oContext) {
                    return oContext.getObject();
                });
                // Array de columnas
                var aColumns = [];
                // Recorremos las columnas
                oTable.getColumns().forEach(function (oColumn) {
                    //Se obtiene el label
                    var oLabel = oColumn.getLabel();
                    // En caso que exista el label, se trae su texto, si no vacío
                    var sLabel = oLabel ? oLabel.getText() : "";
                    aColumns.push({
                        label: sLabel,
                        type: sap.ui.export.EdmType.String
                    });

                });
                //Recorriendo los contextos para obtener los datos
                var aFormattedData = aData.map(function (oItem) {
                    // Array de columna
                    var oRow = {};
                    // Recorremos las columnas
                    aColumns.forEach(function (oCol) {
                        //Creando variable para llegar a ExecutionHourdesde array
                        var vValue = oItem;
                        //Buscar la columna que apunta a ExecutionHour
                    

                        oRow= vValue;
                    });

                    return oRow;

                });
                // Se crea la configuracion del excel
                var oSettings = {
                    workbook: {
                        columns: aColumns
                    },
                    dataSource: aFormattedData,
                    fileName: this.oResourceBundle.getText("nombreExcel") + ".xlsx",
                    worker: false
                };
                //Se genera un excel
                var oSheet = new sap.ui.export.Spreadsheet(oSettings);
                //Se descarga el excel
                oSheet.build().finally(function () {
                    oSheet.destroy();
                });

            }, 
	<IconTabFilter text="{i18n>tipologiaCeco}" key="tipoCeco">
				<t:Table id="tableTipoCeco"	
						rows="{path:'mapfresa>/tipoCeco'}" 
						selectionMode="Multi"
						visibleRowCountMode="Auto"
						threshold="15"
						scrollThreshold="50"
						enableBusyIndicator="true">
					<t:extension>
					<OverflowToolbar style="Clear">						
						<ToolbarSpacer/>
						<!--  INI RJVS 10/04/2026 botones upload y download -->
							<Button
							press= "dowloadExceltipoCeco"
							icon="sap-icon://download"
							/>
							<unified:FileUploader id="UploadtipoCeco" 
							                      buttonOnly="true" 
												  icon="sap-icon://upload" 
												  change="oFileUploadCeco" 
												  buttonText=" " 
												  loadUrl="/dummy" 
												  fileType="xlsx" 
												  sameFilenameAllowed="true" />					
						<!--  FIN RJVS 10/04/2026 botones upload y download -->
						<Button
							icon="sap-icon://add"
							press="onAddRowTCeco" visible="{=${aux>/procesado} ? false : true}"/>
                        <Button
							icon="sap-icon://copy"
							press="onCopyRowTCeco" visible="{=${aux>/procesado}? false : true}"/>
							<Button
							icon="sap-icon://delete"
							press="onDeleteRowTCeco" visible="{=${aux>/procesado}? false : true}"/>
					</OverflowToolbar>
				</t:extension>
				<t:columns>
				<t:Column
						autoResizable="true"
						width="4rem">
						<Label text="" />
						<t:template>
                            <Text text="{mapfresa>_numerico}"/>
						</t:template>
					</t:Column>
					<t:Column
						autoResizable="true"
						width="5rem" visible="false">
						<Label text="{i18n>id}" />
						<t:template>
                            <Text text="{path:'mapfresa>Id',formatter:'.formarIdTCeco'}"/>
                        </t:template>
					</t:Column>
					<t:Column
						autoResizable="true"
						width="5rem"
						>
						<Label text="{i18n>orden}" />
						<t:template>
                            <Input value="{path:'mapfresa>Orden'}"/>
                        </t:template>
					</t:Column>
					<t:Column
						autoResizable="true"
						width="20rem">
						<Label text="{i18n>descripcion}"  required="true" />
						<t:template>
							<Input value="{mapfresa>Description}" fieldGroupIds="fgDescription" editable="{= ${mapfresa>Id}.indexOf('9') === 0 ? true : false}" change="onChangeDescrip" />
						</t:template>
					</t:Column>
					<t:Column 
						autoResizable="true"
						width="20rem">
						<Label text="{i18n>descripcionEng}" />
						<t:template>
                            <Input value="{mapfresa>DescriptionEng}" editable="{= ${mapfresa>Id}.indexOf('9') === 0 ? true : false}" />
						</t:template>
					</t:Column>
					<t:Column
						autoResizable="true"
						width="9rem">
						<Label text="{i18n>fechaRegistro}" />
						<t:template>
							<Text text="{path:'mapfresa>ExecutionDate', type:'sap.ui.model.type.Date', formatOptions:{
								pattern:'dd/MM/yyyy'
							}}" 
									textAlign="End"  />
						</t:template>
					</t:Column>
					<t:Column
						autoResizable="true"
						width="9rem">
						<Label text="{i18n>horaRegistro}" />
						<t:template>
							<Text text="{path:'mapfresa>ExecutionHour', type:'sap.ui.model.odata.type.Time'}" 
									textAlign="End" />
						</t:template>
					</t:Column>
					<t:Column
						autoResizable="true"
						width="9rem">
						<Label text="{i18n>usuarioRegistro}" />
						<t:template>
							<Text text="{path:'mapfresa>Userid'}" 
									textAlign="End" />
						</t:template>
					</t:Column>
				</t:columns>				
			</t:Table>
			</IconTabFilter>
