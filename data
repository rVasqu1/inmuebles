 var oModel = this.getOwnerComponent().getModel("tipologiaCeco");

    if (!oModel) {
        console.log("Model tipologiaCeco non trovato");
        return;
    }

    var aData = oModel.getProperty("/catalogCeco");

    for (var i = 0; i < aData.length; i++) {
        aData[i].numero = i + 1;
    }

    oModel.setProperty("/catalogCeco", aData);
