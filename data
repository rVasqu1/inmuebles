 this.getView().getModel().read("/ZCMSD_FACT_CECOT",{
                success: function (odata) {
                    for (var i in odata.results) {
                        this.getView().getModel().createEntry("/ZCMSD_FACT_CECOT")
                        odata.results[i].numero = (parseInt(i) + 1);
                        
                    }
                    this.getView().getModel("tipologiaCeco").setProperty("/catalogTipoCeco", odata.results);
                }.bind(this)
            });
