sap.ui.define([
    "sap/ui/core/mvc/Controller"
], (Controller) => {
    "use strict";

    return Controller.extend("mapfre.controller.Main", {
        onInit() {
            var oModel = this.getOwnerComponent().getModel("tipologiaCeco");
            var aData = oModel.getProperty("/catalog/models");
            console.log(aData)
        }
    });
});
